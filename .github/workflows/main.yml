name: CI

on: pull_request
jobs:
  reminder:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@main
    - name: Changelog Reminder
      uses: peterjgrainger/action-changelog-reminder@master
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        
        
